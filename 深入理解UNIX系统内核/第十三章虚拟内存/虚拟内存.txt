进程地址空间是由程序可能访问的所有虚拟内存位置组成的。 在任意一个时刻， 进程地址空间和进程的寄存器状态一起构成进程的当前状态。

当进程通过exec系统调用执行新程序时， 内核会为新镜像创建一个新的地址空间。

控制寄存器CR3的高20位里保存了当前页目录的物理页号， 因此它也称为页目录基址寄存器。


进程的一个页在某时刻可能处于以下状态之一：
	驻留
	按需填充
	已换出

fork系统调用创建一个子进程， 包含下面内存管理操作：
	交换空间
	页表
	u区
	代码区
	数据区与栈区
